def get_userlist(what):
    regulator = [{'login': 'regulator@yopmail.com','password':'regulator' }]
    admin = [{'login': 'admin@yopmail.com','password':'password' }]
    name = what
    print(name)
    return name

get_userlist(regulator)
# print(get_userlist(what))